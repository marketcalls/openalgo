"""SIP (Systematic Investment Plan) backtesting.

A SIP invests a fixed amount on a schedule rather than a lump sum on one day,
so its return must be measured with XIRR rather than CAGR -- see ``sip.xirr``.
"""
