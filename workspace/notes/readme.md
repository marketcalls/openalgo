# notes/

Working notes. Broker quirks, parameters that worked, things to revisit.

Not a secrets store — API keys and broker credentials belong in `.env`, which
is gitignored, excluded from Docker images, and expected to be chmod 600.
