# output/

Rendered artifacts: .html charts, .png images, scan result .csv.

Files share the stem of the script that produced them, so
`charts/ema_SBIN_D.py` writes `output/ema_SBIN_D.html`.

Everything here is reproducible by re-running its script — safe to delete
wholesale.
